---
phase: 4
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/pages/NotFoundPage.jsx
  - public/robots.txt
  - public/assets/images/profile-photo-PLACEHOLDER.jpg
  - public/assets/documents/resume-PLACEHOLDER.pdf
  - .github/workflows/deploy.yml
autonomous: true
user_setup: []
must_haves:
  truths: []
  artifacts:
    - src/pages/NotFoundPage.jsx
    - public/robots.txt
    - public/assets/images/profile-photo-PLACEHOLDER.jpg
    - public/assets/documents/resume-PLACEHOLDER.pdf
    - .github/workflows/deploy.yml
  key_links: []
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
  - PL-01
  - PL-02
  - PL-03
  - PL-04
  - PRIV-01
  - PRIV-02
  - PRIV-03
  - DEP-01
  - DEP-02
---

# Phase 4 Plan 01: Error, Placeholders & Docs

## Objective

- **What:** Build the custom NotFoundPage, generate/place the required profile photo and resume PDF placeholder files in public assets, add robots.txt file, and configure standard GitHub Actions pipeline.
- **Why:** Completes all the error fallback requirements, static assets verification, privacy controls, and deployment pipelines.
- **Output:** Fully functional application with all asset placeholders satisfied, robots.txt privacy block, and automated deployment configurations.

## Context

@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-error-placeholders-docs/04-CONTEXT.md

## Tasks

<task type="code">
  <name>Create NotFoundPage component</name>
  <files>src/pages/NotFoundPage.jsx</files>
  <read_first>
    - src/App.jsx
  </read_first>
  <action>
    Create `src/pages/NotFoundPage.jsx` with a premium centered 404 container.
    Features a link button: "Back to About Me" pointing to the home page route `/`.
  </action>
  <acceptance_criteria>
    - `src/pages/NotFoundPage.jsx` exists
    - Renders a clean 404 message and button back to home
  </acceptance_criteria>
  <verify>Check file contents of NotFoundPage.jsx.</verify>
  <done>NotFound page created successfully.</done>
</task>

<task type="code">
  <name>Integrate NotFoundPage in App.jsx</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx
    - src/pages/NotFoundPage.jsx
  </read_first>
  <action>
    - Import `NotFoundPage` from `./pages/NotFoundPage` in `src/App.jsx`.
    - Update the fallback route `path="*"` to render this external component.
  </action>
  <acceptance_criteria>
    - `src/App.jsx` imports `NotFoundPage` from `./pages/NotFoundPage`
    - Fallback route renders `<NotFoundPage />`
  </acceptance_criteria>
  <verify>Check App.jsx to verify imports.</verify>
  <done>NotFound page integrated into app routes.</done>
</task>

<task type="code">
  <name>Create asset directories and placeholder files</name>
  <files>
    - public/assets/images/profile-photo-PLACEHOLDER.jpg
    - public/assets/documents/resume-PLACEHOLDER.pdf
  </files>
  <read_first>
    - src/data/profile.js
  </read_first>
  <action>
    - Create directories `public/assets/images` and `public/assets/documents`.
    - Generate an image for the profile placeholder avatar using generate_image tool, saving to `public/assets/images/profile-photo-PLACEHOLDER.jpg`.
    - Create a text document with description details at `public/assets/documents/resume-PLACEHOLDER.pdf`.
  </action>
  <acceptance_criteria>
    - Directories exist
    - `public/assets/images/profile-photo-PLACEHOLDER.jpg` exists
    - `public/assets/documents/resume-PLACEHOLDER.pdf` exists
  </acceptance_criteria>
  <verify>Check directory presence and verify file existence.</verify>
  <done>Physical assets directories and placeholder files created.</done>
</task>

<task type="code">
  <name>Create robots.txt to disallow crawler indexing</name>
  <files>public/robots.txt</files>
  <read_first>
    - public/index.html
  </read_first>
  <action>
    Create `public/robots.txt` disallowing all user agents from indexing to support the private-style deliverable requirement.
  </action>
  <acceptance_criteria>
    - `public/robots.txt` exists
    - Contains:
      ```
      User-agent: *
      Disallow: /
      ```
  </acceptance_criteria>
  <verify>Check file contents of robots.txt.</verify>
  <done>robots.txt file created blocking all crawlers.</done>
</task>

<task type="code">
  <name>Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - package.json
  </read_first>
  <action>
    Create `.github/workflows/deploy.yml` with automated build and deployment configuration to target GitHub Pages.
  </action>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` exists
    - Implements steps for: Checkout, Setup Node, Install, Build, and Deploy using standard actions (e.g. JamesIves/github-pages-deploy-action).
  </acceptance_criteria>
  <verify>Check file contents of deploy.yml.</verify>
  <done>Deployment actions workflow configured.</done>
</task>

## Verification

- Start Vite dev server.
- Verify navigating to `/invalid-url` renders the custom 404 NotFound page.
- Verify profile photo placeholder renders the newly generated image on the About page.
- Verify the resume download button successfully downloads the placeholder PDF file.
- Verify `public/robots.txt` contains crawler blocking rules.

## Success Criteria

- [ ] NotFoundPage renders at invalid route.
- [ ] Image assets and documents exist in the public directory structure.
- [ ] robots.txt exists with disallow rule.
- [ ] GitHub Actions deploy script exists.
