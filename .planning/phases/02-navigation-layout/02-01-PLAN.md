---
phase: 2
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/components/Navbar.jsx
  - src/components/Footer.jsx
autonomous: true
user_setup: []
must_haves:
  truths: []
  artifacts:
    - src/components/Navbar.jsx
    - src/components/Footer.jsx
    - src/App.jsx
  key_links: []
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
  - FOOT-01
  - FOOT-02
  - FOOT-03
---

# Phase 2 Plan 01: Navigation & Layout

## Objective

- **What:** Extract and build dedicated `Navbar` and `Footer` components, implement fully responsive header layouts (desktop inline links, mobile animated hamburger menu modal overlay, dynamic active route styling), and configure a slim footer containing placeholders and hover-effect social SVGs.
- **Why:** Delivers key UI navigation and branding elements, ensuring professional desktop and mobile responsiveness.
- **Output:** Working, responsive navigation header and slim persistent footer.

## Context

@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-navigation-layout/02-CONTEXT.md

## Tasks

<task type="code">
  <name>Create responsive Navbar component with Active styles and Mobile Menu</name>
  <files>src/components/Navbar.jsx</files>
  <read_first>
    - src/components/Navbar.jsx
    - src/App.jsx
  </read_first>
  <action>
    Create `src/components/Navbar.jsx` with:
    - Translucent glassmorphism header styling: sticky, backdrop-blur-md, top-0, z-50, border-b border-brand-bg-card.
    - Brand logo text on the left linking to `/`.
    - Desktop navigation menu on the right using `NavLink` from `react-router-dom` to automatically style the active link (active: `text-brand-accent-primary`, inactive: `text-brand-text-secondary hover:text-brand-text-primary transition-colors`).
    - Mobile menu hamburger toggle button. Uses a morphing visual style (e.g. dynamic Tailwind classes on lines to rotate into an 'X' when open).
    - Mobile overlay modal: when hamburger is clicked, display a full-width drop-down panel with stacked links, closing the panel automatically when a link is clicked.
  </action>
  <acceptance_criteria>
    - `src/components/Navbar.jsx` exists
    - `NavLink` from `react-router-dom` is used for navigation links
    - Active link receives `text-brand-accent-primary` or equivalent custom class
    - Hamburger button toggle updates local state `isOpen`
    - Clicking mobile menu links triggers state change to close menu
  </acceptance_criteria>
  <verify>Check file contents of Navbar.jsx via view_file.</verify>
  <done>Navbar component created with responsive states and active indicators.</done>
</task>

<task type="code">
  <name>Create Footer component with Social icons</name>
  <files>src/components/Footer.jsx</files>
  <read_first>
    - src/components/Footer.jsx
  </read_first>
  <action>
    Create `src/components/Footer.jsx` with:
    - Footer container styled with a thin border-t border-brand-bg-card/50.
    - Copyright notice text containing the exact placeholders: `© {new Date().getFullYear()} [PLACEHOLDER: Your Name]. All rights reserved.`
    - Inline flex row containing small SVG icon links:
      - GitHub: links to user's profile placeholder (e.g. `https://github.com/` or mailto)
      - LinkedIn: links to user's profile placeholder
      - Email: links using a `mailto:[PLACEHOLDER: Your Email]` protocol
    - SVGs styled to hover-transition from secondary gray to electric blue (`hover:text-brand-accent-primary`) or violet (`hover:text-brand-accent-secondary`) with class `transition-colors duration-300`.
  </action>
  <acceptance_criteria>
    - `src/components/Footer.jsx` exists
    - Footer contains exact placeholder text `[PLACEHOLDER: Your Name]`
    - Footer contains SVGs for GitHub, LinkedIn, and Email
    - Icons have color transition styling on hover
  </acceptance_criteria>
  <verify>Check file contents of Footer.jsx.</verify>
  <done>Footer component is created with placeholders and social SVGs.</done>
</task>

<task type="code">
  <name>Integrate Navbar and Footer into main App Layout</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx
    - src/components/Navbar.jsx
    - src/components/Footer.jsx
  </read_first>
  <action>
    - Clean up `src/App.jsx` by importing `Navbar` and `Footer`.
    - Replace the inline header and footer code in `AppContent` with `<Navbar />` and `<Footer />`.
    - Ensure main page route rendering is wrapped inside the correct layout structure.
  </action>
  <acceptance_criteria>
    - `src/App.jsx` imports `Navbar` and `Footer` components
    - `AppContent` renders `<Navbar />` at top and `<Footer />` at bottom
    - Visual display of home route compiles successfully without errors
  </acceptance_criteria>
  <verify>Check App.jsx to verify integration of Navbar and Footer.</verify>
  <done>Navbar and Footer integrated into main routing container.</done>
</task>

## Verification

- Start Vite dev server via `npm run dev`.
- Verify the header stickiness.
- Test hamburger menu toggle in mobile width.
- Test active styling on different routes.
- Verify footer renders correctly.

## Success Criteria

- [ ] Sticky header has transparent backdrop blur.
- [ ] Active links are highlighted.
- [ ] Hamburger menu works on mobile sizes.
- [ ] Footer copyright matches the exact placeholder schema.
- [ ] Social links hover effects transition smoothly.
