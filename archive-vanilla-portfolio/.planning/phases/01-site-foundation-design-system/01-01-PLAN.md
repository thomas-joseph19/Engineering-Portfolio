---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - css/style.css
  - index.html
  - js/main.js
autonomous: true
user_setup: []
must_haves:
  truths: []
  artifacts:
    - css/style.css
    - index.html
    - js/main.js
  key_links: []
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - SITE-06
  - HOME-01
  - HOME-02
---

# Phase 1 Plan 01: Site Foundation & Homepage

## Objective

- **What:** Build the core static files: CSS design system, homepage HTML layout (hero section, navigation, footer, placeholder sections), and mobile responsive navigation behavior.
- **Why:** Establishes the base layout and typography, matching the "cool technical" styling contract.
- **Output:** A working, responsive homepage page.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-site-foundation-design-system/01-UI-SPEC.md

## Tasks

<task type="code">
  <name>Create CSS Design Tokens and Core Styles</name>
  <files>css/style.css</files>
  <read_first>
  - css/style.css
  </read_first>
  <action>
  Create `css/style.css` containing:
  - Google Font import for 'Inter'
  - `:root` custom properties for colors:
    - `--bg-primary: #f8fafc`
    - `--bg-surface: #ffffff`
    - `--color-text: #0f172a`
    - `--color-text-muted: #94a3b8`
    - `--color-accent: #3b82f6`
    - `--color-dark: #0f172a`
  - Custom properties for spacing (4px, 8px, 16px, 24px, 32px, 48px, 64px)
  - Base utility styles for containers, buttons (primary accent, secondary outlined), flexbox/grid layout helpers
  - Responsive media query helper breakpoints (768px for mobile/tablet boundary)
  </action>
  <acceptance_criteria>
  - `css/style.css` exists
  - `css/style.css` contains `@import url` or link for Inter font
  - `css/style.css` contains custom property values matching UI-SPEC colors
  </acceptance_criteria>
  <verify>Verify file contents using grep/view_file</verify>
  <done>Style tokens and base utilities are defined</done>
</task>

<task type="code">
  <name>Create index.html with Home layout</name>
  <files>index.html</files>
  <read_first>
  - index.html
  - css/style.css
  </read_first>
  <action>
  Create `index.html` with:
  - Document head importing Inter font and `css/style.css`
  - Fixed top navigation bar containing logo/brand "UCF Mechanical Engineering" and links (Home, Projects, About, Resume, Contact) with a mobile hamburger toggle button
  - Hero section (100vh) with:
    - Large heading: Name
    - Subtitle: "Mechanical Engineering Student"
    - Institution: "University of Central Florida"
    - Brief personal statement
    - CTA buttons: "View Projects" (primary) and "Download Resume" (secondary)
  - Placeholder sections below the hero:
    - Featured Projects (empty section with dynamic data alert)
    - Engineering Skills (empty grid)
    - Career Timeline (empty list)
  - Footer with name, copyright, and social links (GitHub, LinkedIn, Email)
  </action>
  <acceptance_criteria>
  - `index.html` exists
  - `index.html` contains `<nav>` with navigation links
  - `index.html` contains header text "Mechanical Engineering Student" and "University of Central Florida"
  - `index.html` contains CTA links / buttons
  </acceptance_criteria>
  <verify>Check HTML markup via view_file or validation</verify>
  <done>index.html created with required semantic structure</done>
</task>

<task type="code">
  <name>Create navigation and page interactivity script</name>
  <files>js/main.js</files>
  <read_first>
  - js/main.js
  - index.html
  </read_first>
  <action>
  Create `js/main.js` with:
  - DOMContentLoaded event listener
  - Mobile hamburger menu toggle handler (toggles a `.nav-open` class on navigation)
  - Window scroll event listener to add an `.opaque` class to the navbar when the page scrolls down past 50px
  - Load the script via `<script src="js/main.js" defer></script>` in `index.html`
  </action>
  <acceptance_criteria>
  - `js/main.js` exists
  - `js/main.js` has event listener for click on hamburger
  - `js/main.js` has scroll event listener
  - `index.html` contains script tag for `js/main.js`
  </acceptance_criteria>
  <verify>Check JavaScript code structure</verify>
  <done>Mobile menu and scroll header interactivity is active</done>
</task>

## Verification

- Start local development server or open `index.html` directly in browser.
- Verify page is responsive by resizing window.
- Verify navigation bar transitions from transparent to opaque on scroll.
- Verify hamburger menu works on mobile resolution.

## Success Criteria

- [ ] `css/style.css` contains all design tokens
- [ ] `index.html` displays UCF, Mechanical Engineering Student, name, CTAs
- [ ] Interactivity in `js/main.js` handles mobile menu and scroll effect
- [ ] Layout is fully responsive
